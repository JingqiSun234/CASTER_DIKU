[project]
name = "caster-baseline-impl"
version = "0.1.0"
description = "Baseline forecasting implementation for CASTER experiments"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
